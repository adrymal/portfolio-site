- Выполнить `git push -u origin main` после настройки авторизации в GitHub Desktop или через терминал.
- При необходимости убрать служебные `_context` файлы из будущих коммитов, если не хотите хранить их в репозитории.
