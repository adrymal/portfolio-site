- Принято: featured-проект определяется автоматически как самая новая работа (`Date desc`) в секции `works`.
- Принято: каждая работа хранится как leaf bundle с локальным `cover.*` ресурсом для масштабируемости модели.
- Принято: Tailwind подключается через Hugo Pipes + PostCSS (`assets/css/main.css` -> `css.PostCSS`), CDN-режим исключён.
- Принято: для индивидуальных страниц работ используется секционный шаблон `layouts/works/single.html`; gallery строится из bundle image-resources с исключением `cover.*`.
- Принято: демонстрационный контент и UI-copy закреплены в направлении graphic designer / art director (branding, identity, campaign, editorial, social/motion-support, presentation).
- Принято: фильтрация на главной реализуется client-side (кнопки категорий + `data-category` на карточках), чтобы сохранить текущую Hugo-структуру и fixed featured-блок.
- Принято: визуальный язык сайта переведен в светлый modern SaaS direction с glassmorphism-панелями, aurora background-акцентами и типографикой `Space Grotesk + DM Sans`.
- Принято: явная обложка проекта задается через front matter `cover`, чтобы bundle мог хранить и новые реальные изображения, и старые SVG-заглушки без конфликта в шаблонах.
- Принято: акцентный маркер на карточке задается через опциональный front matter-параметр `badge`, чтобы его можно было включать и убирать на уровне отдельной статьи.
- Принято: контакты и ссылки футера хранятся в `Site.Params` (`email`, `social.*`), чтобы footer partial оставался переиспользуемым и не содержал хардкода.
- Принято: брендовый логотип в header реализован локально как inline SVG с CSS-анимацией, а не через внешнюю Lottie-зависимость, чтобы избежать сетевой зависимости и упростить поддержку.
